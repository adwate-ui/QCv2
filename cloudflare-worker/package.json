{
  "name": "authentiqc-worker",
  "version": "1.3.0",
  "description": "Cloudflare Worker for AuthentiqC - handles image proxying, metadata extraction, and image diff comparison",
  "type": "module",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "tail": "wrangler tail"
  },
  "dependencies": {
    "pixelmatch": "^5.3.0",
    "pngjs": "^6.0.0",
    "jpeg-js": "^0.4.3"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
