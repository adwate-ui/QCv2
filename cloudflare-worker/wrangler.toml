name = "authentiqc-worker"
main = "index.mjs"
compatibility_date = "2025-12-08"
compatibility_flags = ["nodejs_compat"]
workers_dev = true

# Account ID is automatically detected from the API token
# Hardcoding it here can cause issues if the token belongs to a different account
# If deployment fails, you can explicitly set it with: account_id = "your-account-id"
# Or set the CF_ACCOUNT_ID environment variable in GitHub secrets
